{
  "name": "@ban/orchestrator",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "OLD-build": "tsup src/index.ts --format esm --dts",
    "build": "tsc --project tsconfig.json",
    "dev": "tsx watch src/index.ts || true"
  }
}