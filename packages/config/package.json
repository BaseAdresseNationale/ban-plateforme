{
  "name": "@ban/config",
  "version": "0.1.0",
  "main": "dist/index.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "scripts": {
    "OLD-build": "tsup src --dts",
    "build": "tsup src --format esm --dts"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "zod": "^3.22.4"
  }
}